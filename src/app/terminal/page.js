import Terminal from "../../components/Terminal";

export const metadata = {
  title: "Terminal | Temuulen's Portfolio",
  description: "Terminal mode - Explore my portfolio through commands",
};

export default function TerminalPage() {
  return <Terminal />;
}

